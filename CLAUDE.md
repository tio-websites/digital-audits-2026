@AGENTS.md

## Deployment

All Vercel deployments go to the **TIO** team (`--scope tio-3c1265c3`) unless explicitly told otherwise.

Each new session is treated as a new project — create a new Vercel project per session rather than redeploying to an existing one.
